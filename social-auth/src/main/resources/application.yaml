spring:
  application:
    name: social-auth
  main:
    cloud-platform: kubernetes
  cloud:
    kubernetes:
      config:
        enabled: true
      reload:
        enabled: true
        monitoring-config-maps: true
        strategy: refresh
        mode: event