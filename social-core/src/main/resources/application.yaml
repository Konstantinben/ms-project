spring:
  application:
    name: social-core
  main:
    cloud-platform: kubernetes
  cloud:
    kubernetes:
      config:
        enabled: true
      reload:
        enabled: true
        monitoring-config-maps: true
        strategy: refresh
        mode: event